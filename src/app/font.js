import { Lobster, Oswald } from 'next/font/google';

export const lobster = Lobster({
  subsets: ['latin'],
  display: 'swap',
  weight: ['400'],
});

export const oswald = Oswald({
  subsets: ['latin'],
  display: 'swap',
});
